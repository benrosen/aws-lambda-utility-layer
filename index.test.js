const { expect } = require("@jest/globals");
const { getEnvData, invokeLambda } = require("./index");

describe("The aws-lambda-utility-layer package", () => {
  // TODO test index.js
  it("should fail.", () => {
    expect(true).toBe(false);
  });
});
