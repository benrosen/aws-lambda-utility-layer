const { invokeLambda } = require("./invokeLambda");

describe("The invokeLambda function", () => {
  // TODO test invokeLambda
  it("should arbitrarily pass.", () => {
    expect(true).toBe(true);
  });
});
